services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: activities
      POSTGRES_USER: activities
      POSTGRES_PASSWORD: activities
    ports:
      - "5432:5432"
    volumes:
      - activities-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U activities -d activities"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  activities-db:
