name: activities-discord-bot

services:
  bot:
    build:
      context: ../..
      dockerfile: Dockerfile
    env_file:
      - path: ../../.env
        required: false
    volumes:
      - ../../data:/app/data
    restart: unless-stopped
