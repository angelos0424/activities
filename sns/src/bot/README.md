# Bot

Discord bot presentation layer.

This folder groups bot-facing handlers such as slash commands, component interactions,
modal submissions, and permission checks. Domain logic should live under `src/domains`.
