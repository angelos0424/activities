# Storage Integration

Storage adapter code for the local Activities Discord bot.

Use this folder for persistence boundaries and storage-specific mapping. Domain modules
should depend on storage interfaces rather than storage implementation details.
