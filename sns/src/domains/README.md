# Domains

Business-domain modules used by the SNS Discord bot.

Each domain should own its use cases, validation rules, and domain-specific data mapping.
Discord-specific concerns belong under `src/bot`.
